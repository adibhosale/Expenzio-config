spring:
    application:
        name: expenzio-config-dev
    datasource:
        driver-class-name: org.mariadb.jdbc.Driver
        url: jdbc:mariadb://localhost:3306/expenzio
        username: root
        password: aditya

eureka:
    client:
        fetch-registry: true
        register-with-eureka: true
        service-url:
            defalutZone: https://expenzio-discovery-server-production.up.railway.app/eureka/
    instance:
        prefer-ip-address: true
