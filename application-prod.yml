spring:
    application:
        name: expenzio-config-prod

eureka:
    client:
        fetch-registry: true
        register-with-eureka: true
        service-url:
            defalutZone: https://expenzio-discovery-server-production.up.railway.app/
    instance:
        prefer-ip-address: true
